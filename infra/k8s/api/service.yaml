apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: deployx
  labels:
    app: api
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: deployx-platform
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP

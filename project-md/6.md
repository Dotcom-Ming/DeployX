请实现边缘网关与域名/SSL 系统：

### 边缘网关
基于 Traefik 实现：
1. 动态路由：根据 Host 头匹配项目部署
2. 自定义域名 CNAME 验证
3. 默认域名：<project>-<hash>.deployx.app
4. Preview 域名：<project>-<branch>-<hash>.deployx.app
5. 灰度回滚：支持流量切分到旧版本

### SSL 证书
1. 默认域名通配符证书（手动配置）
2. 自定义域名通过 cert-manager + Let's Encrypt 自动签发
3. DNS-01 challenge（推荐）支持泛域名

### 域名验证流程
用户添加域名 → 显示 CNAME 配置 → 后台轮询 DNS 解析 → 验证通过后下发证书

请输出：
- Traefik IngressRoute CRD 动态生成代码
- 域名验证 Service
- cert-manager Certificate 资源生成
- 前端域名管理页面 UI

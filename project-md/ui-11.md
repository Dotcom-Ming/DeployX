请创建一套统一的状态组件 components/ui/status-*：

### StatusDot
不同状态的小圆点（带脉冲动画）：
- ready（绿色）
- building（蓝色，pulse 动画）
- error（红色）
- queued（黄色）
- canceled（灰色）

### StatusBadge
shadcn Badge 扩展，含图标 + 文字：
- 自动颜色映射
- 支持 size: sm/md
- 支持 variant: solid/soft/outline

### DeploymentStatusTimeline
水平时间线展示部署阶段：
- 已完成（绿色 Check）/ 进行中（蓝色 Spinner）/ 待执行（灰色）/ 失败（红色 X）
- 阶段间连接线
- 每阶段显示耗时

输出完整组件 + Storybook 风格示例。

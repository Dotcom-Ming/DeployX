请生成一个 monorepo 项目结构，使用 pnpm workspace + Turborepo 管理，包含以下子项目：

apps/
  ├── web/              # Next.js 用户控制台
  ├── admin/            # 后台管理面板
  ├── api/              # NestJS 主 API 服务
  ├── builder/          # 构建调度服务
  ├── billing/          # 计费服务
  ├── gateway/          # 边缘网关服务
  └── docs/             # 文档站

packages/
  ├── ui/               # 共享 UI 组件库
  ├── database/         # Prisma schema 和 client
  ├── shared/           # 共享类型与工具
  ├── auth/             # 认证 SDK
  └── sdk/              # 平台 SDK (供用户 CLI 使用)

infra/
  ├── k8s/              # Kubernetes manifest
  ├── terraform/        # 基础设施即代码
  └── docker/           # Dockerfile 集合

请输出完整的 package.json、turbo.json、tsconfig.json 配置，并初始化 ESLint + Prettier + Husky + Commitlint。

请实现 Git 仓库集成与自动构建流水线：

### Git 集成
1. GitHub App 接入（推荐）/ OAuth 方式
2. 监听 push、pull_request 事件 webhook
3. 自动创建 Preview Deployment（PR）和 Production Deployment（main 分支）
4. 在 PR 中评论部署 URL

### 构建流水线
使用 Kubernetes Job + Kaniko 实现安全的容器化构建：

流程：
1. Webhook 触发 → 写入 BullMQ 队列
2. Builder Worker 消费 → 创建 K8s Job
3. Job 内执行：
   - clone 仓库（指定 commit）
   - 检测框架（Next.js/Nuxt/Vite/Static/Node）
   - 生成 Dockerfile（按框架模板）
   - Kaniko 构建镜像并推送到 registry
   - 部署到 Knative Service
4. 实时通过 WebSocket 推送日志到前端
5. 更新 Deployment 状态

请输出：
- builder 服务的核心代码（含框架自动检测器）
- 各框架的 Dockerfile 模板
- K8s Job YAML 模板
- 实时日志推送的 WebSocket Gateway 实现
